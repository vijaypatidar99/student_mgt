
<table class="table table-bordered table-dark">
  <thead>
    <tr>
      <th scope="col">employee_id</th>
      <th scope="col">First</th>
      <th scope="col">Last</th>
      <th  scope="col">Action</th>
       <%#= button_to { link_to "click here to add new employee", action: "new", method: :create } %>
   
     <th  scope="col">Action</th>
     <th  scope="col">Action</th>
     <th  scope="col">Action</th>
    
    </tr>
  </thead>
  <tbody>
  <%Employee.all.each do |employee| %>
    <tr>
    <td><%= employee.id %></td>
    <td><%= employee.first_name %></td>
    <td><%= employee.last_name %></td>
    <td><%#= link_to "view all Details", employee_path(employee) %>
 
    
    <td><%#= link_to "Edit/Update", edit_employee_path(employee) %> </td>
<td><%#= link_to "Delete", employee_path(employee), method: :delete, data: { confirm: "Are you sure to delete employee with id- #{employee.id}?" } %></td>

    </tr>
    <tr>
    <% end %>
  </tbody>
</table>



